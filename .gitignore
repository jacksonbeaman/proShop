# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies - we will have node_modules in our root for server dependencies, as well as our frontend - we want them all to be ignored
node_modules
node_modules/ 
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc  - we don't want to push .env because that will contain all of our environmental variables - can contain sensative info - api keys, uri's
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vscode
